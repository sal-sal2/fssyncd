# fssyncd

A background daemon that watches a directory on one Linux machine and syncs any added or changed files to a directory on another machine over the local network.

# Technologies Used
- C++
- POSIX
- Asio
- CMake
